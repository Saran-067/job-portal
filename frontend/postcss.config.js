// postcss.config.js
export default {
  plugins: [
    require('tailwindcss'), // Ensure that tailwindcss is correctly imported
    require('autoprefixer'), // Ensure that autoprefixer is correctly imported
  ]
};
